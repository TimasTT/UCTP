<!DOCTYPE html>
<%#include "objects/studentsgroup.h" %>
<html>
<head>
  <meta charset="UTF-8">
  <title><%= controller()->name() + ": " + controller()->activeAction() %></title>
</head>
<body>

<h1>Listing Students Group</h1>

<%== linkTo("Create a new Students Group", urla("create")) %><br />
<br />
<table border="1" cellpadding="5" style="border: 1px #d0d0d0 solid; border-collapse: collapse;">
  <tr>
    <th>ID</th>
    <th>GroupName</th>
  </tr>
<% tfetch(QList<StudentsGroup>, studentsGroupList); %>
<% for (const auto &i : studentsGroupList) { %>
  <tr>
    <td><%= i.id() %></td>
    <td><%= i.groupName() %></td>
    <td>
      <%== linkTo("Show", urla("show", i.id())) %>
      <%== linkTo("Edit", urla("save", i.id())) %>
      <%== linkTo("Remove", urla("remove", i.id()), Tf::Post, "confirm('Are you sure?')") %>
    </td>
  </tr>
<% } %>
</table>

</body>
</html>
